#include<stdio.h>
 
 struct book
 {
 	char title[20];
 	char author[20];
 	float price;
 }b[5];
 
 main()
 {
 	int i;
 	for (i=0;i<5;i++)
 	{
 		scanf("%s%s%f",b[i].title,b[i].author,&b[i].price);
	 }
	for (i=0;i<5;i++)
 	{
 		printf("%s%s%f",b[i].title,b[i].author,b[i].price);
	 }
	
 }
